---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: liqoupgrades.upgrade.liqo.io
spec:
  group: upgrade.liqo.io
  names:
    kind: LiqoUpgrade
    listKind: LiqoUpgradeList
    plural: liqoupgrades
    singular: liqoupgrade
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.currentVersion
      name: Current
      type: string
    - jsonPath: .spec.targetVersion
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiqoUpgrade is the Schema for the liqoupgrades API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiqoUpgradeSpec defines the desired state of LiqoUpgrade
            properties:
              currentVersion:
                description: CurrentVersion is the current Liqo version (e.g., "v1.0.0")
                type: string
              namespace:
                default: liqo
                description: 'Namespace where Liqo is installed (default: "liqo")'
                type: string
              remoteClusterVersions:
                description: |-
                  RemoteClusterVersions lists the Liqo versions of remote peered clusters (optional)
                  If not provided, the controller assumes remote clusters are running the same version as local cluster
                items:
                  description: RemoteClusterVersion represents a remote cluster's
                    Liqo version
                  properties:
                    clusterID:
                      description: ClusterID is the unique identifier of the remote
                        cluster
                      type: string
                    version:
                      description: Version is the Liqo version running on the remote
                        cluster
                      type: string
                  required:
                  - clusterID
                  - version
                  type: object
                type: array
              targetVersion:
                description: TargetVersion is the desired Liqo version (e.g., "v1.0.1")
                type: string
            required:
            - currentVersion
            - targetVersion
            type: object
          status:
            description: LiqoUpgradeStatus defines the observed state of LiqoUpgrade
            properties:
              backupName:
                description: BackupName references the backup job created before upgrade
                type: string
              backupReady:
                description: BackupReady indicates if backup is available for rollback
                type: boolean
              compatibilityCheckPassed:
                description: CompatibilityCheckPassed indicates if compatibility check
                  succeeded
                type: boolean
              conditions:
                description: conditions represent the current state of the LiqoUpgrade
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              detectedLocalVersion:
                description: DetectedLocalVersion is the actual version detected on
                  the local cluster
                type: string
              lastSuccessfulPhase:
                description: LastSuccessfulPhase is the last phase that completed
                  successfully
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update
                format: date-time
                type: string
              lowestVersion:
                description: LowestVersion is the lowest version among local and remote
                  clusters
                type: string
              message:
                description: Message provides human-readable details about the current
                  phase
                type: string
              phase:
                description: Phase indicates the current phase of the upgrade
                type: string
              rolledBack:
                description: RolledBack indicates if this upgrade was rolled back
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
