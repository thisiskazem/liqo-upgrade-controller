---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: liqoupgradebackups.upgrade.liqo.io
spec:
  group: upgrade.liqo.io
  names:
    kind: LiqoUpgradeBackup
    listKind: LiqoUpgradeBackupList
    plural: liqoupgradebackups
    shortNames:
    - liqobackup
    singular: liqoupgradebackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.upgradeName
      name: Upgrade
      type: string
    - jsonPath: .spec.phase
      name: Phase
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiqoUpgradeBackup stores backup state for rollback
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiqoUpgradeBackupSpec defines the backup data
            properties:
              backupTime:
                description: BackupTime when the backup was created
                format: date-time
                type: string
              crdManifests:
                additionalProperties:
                  type: string
                description: CRDManifests stores the YAML manifests of CRDs
                type: object
              deploymentManifests:
                additionalProperties:
                  type: string
                description: DeploymentManifests stores deployment manifests (for
                  future phases)
                type: object
              phase:
                description: Phase indicates which phase this backup is for
                type: string
              upgradeName:
                description: UpgradeName references the LiqoUpgrade this backup belongs
                  to
                type: string
              version:
                description: Version is the version backed up
                type: string
            required:
            - backupTime
            - phase
            - upgradeName
            - version
            type: object
          status:
            description: LiqoUpgradeBackupStatus defines the observed state
            properties:
              message:
                description: Message provides details
                type: string
              ready:
                description: Ready indicates if backup is ready for rollback
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
