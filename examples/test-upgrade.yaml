apiVersion: upgrade.liqo.io/v1alpha1
kind: LiqoUpgrade
metadata:
  name: liqo-upgrade-test
  namespace: liqo
spec:
  targetVersion: v1.0.1
  namespace: liqo

  # Note: The controller automatically detects the current version from liqo-controller-manager
  # and retrieves remote cluster versions from ForeignCluster CRs